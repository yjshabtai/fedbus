<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email %>
  </div>
  <div class="field">
	<%= f.label :student_number %><br />
	<%= f.password_field :student_number %>
  </div>
  <div class="field">
	<%= f.label :student_number_confirmation %><br />
	<%= f.password_field :student_number_confirmation %>
  </div>
  <br />

  <!-- <% if logged_in? %>
    <div class="field">
      <%= f.label :table_count, "Rows per table" %><br />
      <%= f.text_field :table_count %>
    </div>
  <% end %>
  
  <% if logged_in? && current_user.has_permission?(:roles) %>
	<p>
	  Roles<br />
	  <% for role in Role.all %>
		<%= check_box_tag "user[role_ids][]", role.id, @user.role_ids.include?(role.id) %><%= role.name %><br />
	  <% end %>
	</p>
  <% end %>-->
  
  <div class="actions">
    <%= f.submit %>
  </div>
  
  
  
<% end %>

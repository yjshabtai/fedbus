source 'https://rubygems.org'

# Newest stable release should go here
gem 'rails', '3.2.0'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

# Sqlite3 is used in development as its the Rails simple default
gem 'sqlite3'

# mysql2 is used for pruduction databases
gem 'mysql2'


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem "meta_search",    '>= 1.1.0.pre'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer'

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# CAS authentication
gem 'rubycas-client'

# Helps greatly with the awesome admin panel, newest stable release
gem 'activeadmin'

# Used to get rid of web prefixes in the sass/scss
gem 'bourbon'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
